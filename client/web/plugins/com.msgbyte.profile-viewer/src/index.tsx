import { registerCoreUserExtraInfo } from '@capital/common';

const PLUGIN_ID = 'com.msgbyte.profile-viewer';

console.log(`Plugin Profile Viewer(${PLUGIN_ID}) is loaded`);

registerCoreUserExtraInfo();
